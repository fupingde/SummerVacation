<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application>
        <activity
            android:name="com.example.module.ui.MydownloadActivity"
            android:exported="false" />
        <activity
            android:name="com.example.module.ui.MycollectedActivity"
            android:exported="false" />
        <!-- android:allowBackup="true" -->
        <!-- android:icon="@mipmap/ic_launcher" -->
        <!-- android:label="@string/app_name" -->
        <!-- android:roundIcon="@mipmap/ic_launcher_round" -->
        <!-- android:supportsRtl="true" -->
        <!-- android:networkSecurityConfig="@xml/network_security_config" -->
        <!-- android:theme="@style/Theme.SummerVacation"> -->
        <activity
            android:name="com.example.module.ui.SongNullActivity"
            android:exported="false" />

        <service
            android:name="com.example.module.ui.services.MusicService"
            android:exported="false" />

        <activity android:name="com.example.module.ui.MusicPlayActivity" />
        <activity
            android:name="com.example.module.ui.activities.SongListActivity"
            android:exported="false" />
        <activity
            android:name="com.example.module.ui.MainActivity"
            android:exported="false">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
    </application>

</manifest>